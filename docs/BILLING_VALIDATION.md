# 计费验证报告（BILLING VALIDATION）

验证日期：2026-08-17（UTC+8 环境）
价格规则版本：`2026-08-17.1`（核验时间 2026-08-17T11:39:00+08:00，来源官方价格页）
计价单位：nanoYuan（1e-9 元），内部全程整数累计

## 1. 固定算例（期望值由已审核 Fixture 手工写出，程序独立计算）

| 算例 | 输入（tokens） | 时段 | 期望 | 实际 | 误差 |
|---|---|---|---|---|---|
| V4 Flash 高峰 | hit1M / miss1M / out1M | 高峰 | 12.10 元 | 12.10 | 0 |
| V4 Flash 闲时 | hit1M / miss1M / out1M | 闲时 | 6.05 元 | 6.05 | 0 |
| V4 Pro 高峰 | hit1M / miss1M / out1M | 高峰 | 36.30 元 | 36.30 | 0 |
| V4 Pro 闲时 | hit1M / miss1M / out1M | 闲时 | 18.15 元 | 18.15 | 0 |
| 全部命中 | hit1M / miss0 / out0 | 闲时 | 0.05 元 | 0.05 | 0 |
| 全部未命中 | hit0 / miss1M / out0 | 闲时 | 1.50 元 | 1.50 | 0 |
| 混合 | hit100k / miss200k / out300k | 高峰 | 3.31 元 | 3.31 | 0 |
| 输出为 0 | hit100k / miss100k / out0 | 闲时 | 0.155 元 | 0.155 | 0 |
| 历史规则（8/17 前） | hit1M / miss1M / out1M | flat | 3.02 元 | 3.02 | 0 |
| 3000 笔小额 | 每笔 6050 nano | 闲时 | 0.018150 元 | 0.018150 | 0（整数累计）；每笔取整法误差 0.0181 元 |

## 2. 时间边界（北京时间，左闭右开）

08:59:59→闲时 ✓ 09:00:00→高峰 ✓ 11:59:59→高峰 ✓ 12:00:00→闲时 ✓
13:59:59→闲时 ✓ 14:00:00→高峰 ✓ 17:59:59→高峰 ✓ 18:00:00→闲时 ✓
在 TZ=UTC / Asia/Tokyo / America/New_York 下 21+4 项测试全部通过，结果与测试机时区无关。

## 3. DSH 集成测试（合成 fixture）

对 `test/fixtures/synthetic-session.assistant-messages.json`（**完全合成**：种子化 PRNG
确定性生成，47 条 assistant/message，全部含原始 usage，provider=deepseek-official，
model=deepseek-v4-flash；前 2 条在 8/17 峰谷价生效前 = 历史价，其余 45 条在北京时间
高峰时段 = 峰谷价；不含任何真实会话标识、消息正文或个人数据）折叠：

- 投影合计：1,839,462,320 nano = ¥1.839462（测试内冻结字面量）
- 时段分布：45 笔高峰 + 2 笔历史 flat —— 版本化规则按调用时刻生效
- 分桶：缓存命中 ¥1.193335 / 未命中 ¥0.041439 / 输出 ¥0.604688
- 重复折叠同一事件列表金额一致（刷新/恢复语义）；追加无 usage 事件金额不变（不虚构费用）
- 开发期曾用运行中 DSH 的真实会话做内部核验（投影与独立复核零误差），该数据未进入仓库；fixture 已全部替换为合成数据。

## 4. 官方余额真实对账

脚本：`scripts/reconcile-official-balance.mjs`
- 流程：读取 `/user/balance` 前余额 → 3 次受控请求（记录原始 usage）→ 本地计费 →
  轮询官方余额更新（超时 120s）→ 比较余额变化与本地费用 → 输出逐次明细/合计/绝对与相对误差。
- 安全：密钥仅从 `DEEPSEEK_API_KEY` 环境变量读取，不打印、不保存；报告中无请求内容。
- DRY-RUN（脚本化 mock，官方侧同样按整数 nano 扣减）验证流程与算术：绝对误差 0.000000000 元，相对误差 0.0000%。

**真实对账状态：未执行** —— 需要用户在专用测试账户上设置 `DEEPSEEK_API_KEY` 运行：

```bash
DEEPSEEK_API_KEY=sk-xxx npm run reconcile
```

验收目标（待真实运行后回填）：内部算术误差 ≤ 1e-9 元（整数累计天然满足）；
与官方余额变化差异原则上 ≤ ¥0.01；若超差需给出原因（余额更新延迟/并发调用/
赠送余额扣减顺序），禁止修改测试容差掩盖。

## 5. 安装 / 卸载 / 干净 Profile 验证

- `loadProfile`（dsh-app-boot 真实启动代码）在含/不含本插件的 bundle 层下均通过（已验证含插件场景；卸载=从 bundles 列表移除 + 删除 node_modules 条目，启动路径相同）。
- 干净安装、重启、显示、卸载、再次启动的**真实端到端验证未在本机完成**（需要用户重启 DSH 并操作）——发布前需完成。
- 客户端 bundle 已通过 ModuleLoader 模拟装载 + 三态渲染测试（约 ¥2.19 / 约 ¥0.00 / 费用 —，无警告）。

## 结论

本地计费的算术与规则选择有测试与真实数据支撑；**真实余额对账、干净安装/卸载恢复未完成前，不宣称可以公开发布**。
